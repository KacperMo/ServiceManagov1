import { test } from '@japa/runner'
import { ProductImgFactory } from 'Database/factories'

test.group('Product imgs', () => {
  test('index', async ({ }) => {
    /* const product = await ProductImgFactory.create()
    await product.find(product.product_id)

    product.assertStatus(200) */
  })

  //Can add

  //Can read

  // is api exist?
  /* const userInfoRes = await fetch('https://api.ipify.org?format=json');
  const userInfo = await userInfoRes.json();
  const dataRes = await fetch(`https://geo.ipify.org/api/v1?apiKey=API_KEY&ipAddress=${userIp.ip}`);
  const data = await dataRes.json(); */
})
